class Post {
    constructor(post) {
        this.id = post.id;
        this.dateCreated = post.dateCreated;
        this.userId = post.body;
        this.type = post.type;
        this.commentsNum = post.body;
    }
}

export default Post;